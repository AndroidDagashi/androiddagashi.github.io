PR を作成する際、セッション中に生じた `.claude/` 配下の変更（`settings.json`、`rules/` など）を「無関係なハウスキーピング」として除外しないこと。原則として PR に含める。

- 移行・修正などの主目的と直接関係しなくても、その作業中に生じた `.claude/` の変更はワーキングツリーに放置せず、同じ PR にまとめる（必要なら別コミットに分ける）。
- 除外したい明確な理由がある場合のみ、ユーザーに確認してから除外する。
